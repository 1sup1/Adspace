# Adaptive Space — MVP 실행계획 및 Android XR 데모 전략

| 항목 | 내용 |
|---|---|
| 기준 문서 | `Adaptive Space — 개인 웰니스 환경 패스포트_기능명세서_2026-08-13.md` |
| 작성일 | 2026-08-13 |
| 목표 | 해커톤용 핵심 사용자 여정과 구현 우선순위를 확정하고 Android XR의 데모 역할을 정의한다. |

---

## 1. 결론

Adaptive Space의 MVP는 **더미 웨어러블 컨디션을 환경 프로필로 변환하고, 집에서 보정한 설정을 원본 생체정보 없이 제휴 공간에 재현하는 데모**다.

발표에서 반드시 증명할 내용은 다음 네 가지다.

1. 컨디션에 따라 조명·온도·사운드 프로필이 달라진다.
2. 사용자의 수정이 다음 추천에 반영된다.
3. 집 프로필이 제휴 공간의 지원 범위에 맞게 변환된다.
4. 제휴 공간에는 생체정보가 전달되지 않고, 사용자가 언제든 중지·복원·만료할 수 있다.

Android XR은 이 중 **공간 변화의 몰입감**을 강조하는 보조 클라이언트로 사용한다. 전체 MVP를 Android XR에 종속시키지 않는다.

### 권장 구현안

- 주 사용자 앱: 모바일 반응형 웹 또는 일반 Android 앱
- 기준 데이터 및 로직: 플랫폼과 독립된 환경 프로필·공간 사양·세션 모델
- 필수 시뮬레이터: 노트북에서 실행 가능한 2D/간단한 3D 방 시뮬레이터
- 선택 쇼케이스: Android XR에서 동일한 환경 프로필을 받아 공간 프리셋을 전환
- 실제 기기: 가능하면 스마트 전구 한 종류만 연동

---

## 2. 개발 전에 확정할 사항

| 결정 | 권장안 | 이유 |
|---|---|---|
| 주 사용자 앱 | 모바일 반응형 웹 | 배포와 시연 복구가 빠르다. |
| 추천 방식 | 확률 모델의 신호 맥락 판단 + 결정론적 값 생성 | 복합 신호 해석력과 실행값의 재현성·안전성을 함께 확보한다. |
| 실제 IoT | 스마트 전구 한 종류 | 기기 연동 범위 확장을 막는다. |
| 체크인 | QR만 구현 | NFC·위치 감지는 핵심 검증에 불필요하다. |
| 데이터 | 더미 사용자와 더미 웨어러블 값 | 실제 민감정보 처리와 OAuth를 피한다. |
| XR 역할 | 제휴 공간 시각화 전용 | XR 장애가 전체 사용자 여정을 막지 않게 한다. |

추가로 팀 내부에서 아래 항목을 바로 결정한다.

- Android XR 헤드셋 실물 보유 여부
- Android/Jetpack Compose 또는 Unity 경험자 유무
- 스마트 전구 제조사와 로컬 제어/API 가능 여부
- 시연 장소의 네트워크 제한 및 외부 API 사용 가능 여부
- 발표 시간과 실제 체험 가능한 인원 수

---

## 3. P0 — 반드시 구현할 기능

### P0-1. 더미 컨디션 입력

회복 필요, 집중 필요, 안정 필요의 세 가지 시나리오를 제공한다.

각 시나리오는 다음 필드를 가진다.

```json
{
  "source": "demo",
  "sleepScore": 52,
  "activity": 3200,
  "heartRate": 78,
  "hrv": 31,
  "timeOfDay": "evening"
}
```

완료 조건:

- 사용자가 시나리오를 선택할 수 있다.
- 화면에 `Demo data` 출처가 표시된다.
- 동의하지 않은 항목은 추천 입력에서 제외된다.
- 실제 건강정보 없이 전체 데모를 실행할 수 있다.
- 더미 Provider가 sequence와 timestamp를 가진 샘플을 일정 간격으로 방출하고 앱이 rolling window를 갱신한다.
- 실제 웨어러블 Provider도 같은 스트림 계약을 구현해 UI·에이전트·정책 코드를 변경하지 않고 교체할 수 있다.

#### 실시간 연동 증명 단계

```text
DemoWearableProvider (timer 기반 AsyncStream)
→ SignalSample(sequence, capturedAt, consented metrics)
→ iOS rolling window와 연결 상태 표시
→ 로컬 추세 요약
→ Agents SDK 맥락 재평가
→ 변화 임계값·cooldown
→ 사용자 승인 또는 허용 범위 내 적용
```

해커톤에서는 “실제 실시간 생체 연동”이 아니라 **실시간 계약을 따르는 더미 연속 스트림**으로 표현한다. 후속 HealthKit 구현은 `HKObserverQuery`로 변경을 감지하고 `HKAnchoredObjectQuery`로 새 샘플만 가져오며, 더 높은 빈도의 Apple Watch 데이터가 필요하면 Watch 앱과 `WatchConnectivity` 어댑터를 추가한다. 데이터 제공 주기는 지표와 운영체제 정책에 따라 다르므로 모든 지표가 초 단위로 갱신된다고 주장하지 않는다.

### P0-2. 컨디션 해석과 환경 프로필 생성

환경 프로필은 제조사 명령이 아닌 공통 값으로 만든다.

```json
{
  "profileId": "profile-001",
  "context": "recovery",
  "confidence": 0.86,
  "lighting": {
    "brightnessPercent": 35,
    "colorTemperatureK": 2700
  },
  "temperatureC": 23,
  "soundPreset": "calm",
  "reason": "수면 점수가 낮고 저녁 시간대이므로 편안한 환경을 제안합니다."
}
```

구현 원칙:

- 에이전트는 동의된 최근 시간순 요약값을 분석 도구로 확인하고 회복·집중·안정 맥락, 신뢰도, 근거를 구조화해 반환한다.
- 핵심 실행값은 에이전트가 아니라 결정론적 정책이 맥락과 사용자 보정값을 입력받아 생성한다.
- 에이전트 출력에는 조명·온도·사운드 실행값이나 제조사 명령을 포함하지 않는다.
- 회복·집중·안정의 결과가 시각적으로 명확히 달라야 한다.
- 신뢰도가 낮거나 입력값이 부족하면 명시적 승인을 요구한다.
- 질환, 스트레스 상태, 감정을 단정하지 않는다.

#### 결정론적·확률론적 모델 비교

| 방식 | 장점 | 한계 | Adaptive Space 적용 |
|---|---|---|---|
| 결정론적 모델만 사용 | 같은 입력에 같은 결과, 테스트·감사·복원이 쉬움 | 복합 신호, 상충 신호, 새로운 맥락에 경직됨 | 실행값 제한, 공간 호환성, 승인, 복원, 만료의 기준선 및 최종 게이트 |
| 확률론적 모델만 사용 | 여러 신호와 시간 맥락을 유연하게 해석하고 근거를 설명함 | 출력 변동, 환각, 범위 초과와 재현성 문제가 있음 | 단독 기기 제어에는 사용하지 않음 |
| 하이브리드 | 해석의 유연성과 실행의 재현성을 분리함 | 구성 복잡도와 모델 비용·지연이 추가됨 | 에이전트가 맥락을 판단하고 정책 엔진이 실행값을 확정하는 현재 방식 |

경쟁력은 “AI가 더 똑똑하다”는 문구가 아니라 아래 비교 평가로 증명한다.

1. 명확한 신호에서는 규칙 단독 기준선과 하이브리드가 같은 안전한 결과를 내는지 확인한다.
2. 상충하거나 변화 추세가 있는 신호에서는 에이전트가 어떤 근거로 맥락을 선택했는지 평가한다.
3. 같은 에이전트 결정을 반복 실행해 정책 엔진의 실행값이 항상 동일한지 확인한다.
4. 모델 실패·타임아웃·스키마 위반 시 결정론적 fallback으로 전체 데모가 완료되는지 확인한다.
5. 비동의 항목이 도구 입력과 모델 입력에서 모두 제거되는지 확인한다.

완료 기준:

- 규칙 단독 기준선과 하이브리드 결과를 동일한 평가 케이스에서 비교한다.
- 에이전트가 실행값을 출력하거나 정책 범위를 우회한 성공 건수는 0건이어야 한다.
- 모델 장애 시 fallback 완료율은 100%여야 한다.
- 맥락 정확도뿐 아니라 근거 일치율, 평균·p95 지연, 호출 비용을 함께 기록한다.

#### 판단 흔들림 방지

확률 모델의 새 판단은 즉시 기기값이 되지 않는다. 아래 결정론적 전이 조건을 모두 통과해야 한다.

- 최근 연속 window에서 동일 맥락이 정해진 횟수 이상 유지된다.
- 현재 맥락과의 confidence 차이가 최소 임계값을 넘는다.
- 마지막 적용 이후 cooldown이 끝났다.
- 새 실행값의 변화량이 최소 체감 임계값을 넘고 사용자 허용 범위 안이다.
- 첫 적용, 낮은 confidence, 큰 변화는 항상 사용자 승인을 요구한다.

이 구조에서 에이전트의 출력은 제안이고, 상태 전이·debounce·hysteresis·cooldown·범위 제한은 모두 재현 가능한 정책이다.

#### 환경 효과 검증 경계

문헌은 빛의 양·스펙트럼·시간대가 생체리듬·단기 각성과 관련되고, 높은 야간 온도와 환경 소음이 수면·과제 수행을 방해할 수 있다는 점을 지지한다. 그러나 `밝기 35%·2700K·23°C·calm`이라는 Adaptive Space 프로필이 모든 사용자의 회복이나 집중을 향상한다는 근거는 아니다. 밝기 비율과 Kelvin만으로 melanopic dose를 알 수 없고, 보편적인 최적 실내 온도도 도출되지 않았다. 백색·분홍 소음의 수면 개선 근거는 매우 낮고 연구 간 이질성이 크다.

해커톤에서는 “회복시킨다”가 아니라 “환경 방해요인을 줄이고 사용자가 선호하는 조건으로 조율한다”고 표현한다. 제품 효과는 정적 기본값과 개인화 프로필을 비교하는 무작위 교차 실험으로 검증한다.

- 1차 지표: 적용 후 수동 재조정률, 유지율, 주관적 편안함·집중 도움 점수
- 탐색 지표: 웨어러블 수면 시작 시간·수면 효율, 표준화된 짧은 주의력 과제
- 안전 지표: 즉시 중지·복원율, 허용 범위 초과 0건, 환경 변경 빈도
- 분석 원칙: 개인 내 비교를 우선하고 진단·치료 효과로 해석하지 않는다.

근거:

- [Recommendations for daytime, evening, and nighttime light exposure](https://doi.org/10.1371/journal.pbio.3001571)
- [Light and short-term alertness meta-analysis](https://pubmed.ncbi.nlm.nih.gov/35142669/)
- [Ambient heat and sleep systematic review](https://pubmed.ncbi.nlm.nih.gov/38598988/)
- [Indoor temperature and cognitive performance meta-analysis](https://doi.org/10.1016/j.buildenv.2024.112488)
- [WHO evidence review on environmental noise and sleep](https://doi.org/10.3390/ijerph15030519)
- [Continuous white noise and sleep systematic review](https://pubmed.ncbi.nlm.nih.gov/33007706/)

### P0-3. 환경 미리보기·적용·복원

방 시뮬레이터는 다음 상태를 표현한다.

- 조명 밝기
- 조명 색온도
- 실내 온도
- 사운드 프리셋

완료 조건:

- 적용 전 변경값과 이유를 확인할 수 있다.
- 적용 직전 상태를 저장한다.
- 적용, 즉시 중지, 적용 전 상태 복원이 동작한다.
- 범주별 성공·실패를 따로 표시한다.
- 한 범주의 실패가 다른 범주의 성공을 취소하지 않는다.

### P0-4. 사용자 피드백과 개인 보정

사용자는 적용 결과에 대해 유지·수정·거부할 수 있다.

MVP 보정식은 단순하게 유지한다.

```text
다음 추천값 = 컨디션 기본값 + 사용자별 보정값
```

완료 조건:

- 사용자가 밝기·온도·사운드를 수정할 수 있다.
- 수정값이 사용자별 보정값으로 저장된다.
- 같은 컨디션을 다시 실행하면 변경 전·후 값을 비교할 수 있다.
- 이를 모델 재학습이나 의료적 효과로 설명하지 않는다.

### P0-5. QR 체크인과 공간 호환성

MVP에서는 QR이 공간 ID를 전달하는 용도로만 동작해도 된다.

```json
{
  "spaceId": "hotel-demo-room",
  "lighting": {
    "supported": true,
    "brightnessRange": [20, 100],
    "colorTemperatureSupported": false
  },
  "temperature": {
    "supported": true,
    "rangeC": [20, 24]
  },
  "sound": {
    "supported": false
  },
  "defaults": {
    "brightnessPercent": 70,
    "temperatureC": 22
  }
}
```

### P0-6. 공간별 프로필 변환

변환기는 다음 순서로 동작한다.

1. 지원하지 않는 범주를 제외한다.
2. 지원 범위를 벗어난 값은 안전 범위 안으로 제한한다.
3. 원본값과 변환값을 비교해 보여준다.
4. 사용자 승인 후 실행값만 제휴 공간에 전달한다.

예시:

| 범주 | 집 프로필 | 호텔 지원 | 호텔 실행값 |
|---|---:|---|---:|
| 밝기 | 35% | 20~100% | 35% |
| 색온도 | 2700K | 미지원 | 제외 |
| 온도 | 23°C | 20~24°C | 23°C |
| 사운드 | Calm | 미지원 | 제외 |

제휴 공간에 전달 가능한 필드:

- 일회성 세션 ID
- 공간 ID
- 환경 실행값
- 만료 시각

전달 금지 필드:

- 수면 점수, 활동량, 심박, HRV
- 컨디션 분류와 추천 이유
- 상세 사용자 선호 이력

### P0-7. 일회성 세션과 체크아웃

완료 조건:

- 세션 시작 시 만료 시각을 생성한다.
- 사용자가 언제든 세션을 종료할 수 있다.
- 종료 시 공간 기본값을 복원한다.
- 종료·만료된 세션 ID로 조회하거나 제어할 수 없다.
- 외부 공간의 환경 실행값 사본이 삭제된 상태를 표시한다.

---

## 4. 필요한 화면

최소 화면 구성은 다음과 같다.

1. 시작 및 데이터 이용 동의
2. 더미 컨디션 선택
3. 현재 컨디션 브리핑
4. 환경 프로필 미리보기
5. 집 환경 적용·중지·복원
6. 적용 결과 수정 및 피드백
7. QR 체크인과 공간 호환성 확인
8. 집 프로필·호텔 실행값 비교
9. 호텔 공간 적용·중지
10. 체크아웃·기본값 복원·세션 만료 확인

모든 제어 화면에는 즉시 중지 또는 복원으로 이동하는 명확한 동선을 둔다.

---

## 5. Android XR 활용 판단

### 결론: 조건부 추천

Android XR은 Adaptive Space와 콘셉트 궁합이 좋다. 조명 색감, 공간 분위기, 사운드 변화를 사용자가 직접 둘러보게 할 수 있어 정적인 모바일 화면보다 공간 개인화를 강하게 전달한다.

다만 **유일한 시뮬레이터 또는 주 사용자 앱으로 사용하는 것은 권장하지 않는다.**

### 잘 맞는 이유

- 집과 호텔이라는 두 공간의 전환을 몰입형 장면으로 보여줄 수 있다.
- 적용 전·후 조명과 분위기 차이를 설명 없이 체감시키기 쉽다.
- Android XR의 Full Space에서 사용자 주변 환경을 교체할 수 있다.
- 공간 패널로 환경값, 호환성, 프라이버시 전달 범위를 함께 표시할 수 있다.
- 실제 IoT 장비가 부족해도 공간 변화라는 제품 결과를 시각화할 수 있다.

### 주 데모로 두면 위험한 이유

- 온도 변화는 XR 화면만으로 실제 체감시키기 어렵다.
- 생체정보 미전달, 세션 권한, 체크아웃 만료는 별도 UI 증거가 필요하다.
- 헤드셋을 쓴 한 명만 볼 수 있어 심사위원 전체에게 같은 장면을 전달하기 어렵다.
- 에뮬레이터만 화면에 띄우면 XR의 몰입 장점은 줄고 조작 복잡도는 남는다.
- 공간 환경, 3D 자산, 입력 방식과 성능 문제까지 디버깅 범위가 커진다.
- Android XR 공간 UI와 일부 공간 기능은 계속 변화할 수 있는 프리뷰 성격을 가진다.

### Android XR을 채택할 조건

아래 조건 중 대부분을 만족하면 XR 쇼케이스를 만든다.

- 실물 Android XR 헤드셋 또는 안정적인 에뮬레이터 환경이 이미 있다.
- 팀에 Jetpack Compose/Android 또는 Unity 경험자가 있다.
- 핵심 모바일·웹 데모가 먼저 완성되어 있다.
- 발표 화면으로 헤드셋 미러링이 가능하다.
- 3D 방 자산을 새로 크게 제작하지 않고 기존 GLB/glTF 또는 단순 프리셋을 사용할 수 있다.

조건을 만족하지 않으면 일반 웹 3D 방 시뮬레이터가 더 안전하다.

### 권장 XR 데모 범위

XR은 다음 한 장면에만 집중한다.

```text
호텔 QR 체크인
→ 집 프로필과 호텔 지원 범위 표시
→ “생체정보 0건 전달” 확인
→ 적용 승인
→ 호텔 방의 조명·분위기·사운드 전환
→ 중지 또는 체크아웃
→ 기본 환경 복원
```

XR 화면 구성:

- 중앙: 호텔 방 또는 웰니스 공간
- 왼쪽 공간 패널: 집 프로필
- 오른쪽 공간 패널: 호텔에서 실행 가능한 변환값
- 하단 제어: 적용, 즉시 중지, 기본값 복원
- 상태 배지: `Biometric data shared: 0`, 세션 남은 시간

### 구현 전략

1. 환경 프로필과 공간 사양을 공통 JSON으로 정의한다.
2. 모바일·웹 앱이 추천, 피드백, QR 체크인을 담당한다.
3. XR 클라이언트는 승인된 실행 프로필만 수신한다.
4. 회복·집중·안정별로 미리 준비한 공간 프리셋을 전환한다.
5. 조명 밝기와 색감은 프리셋 또는 3D 장면 파라미터로 표현한다.
6. 온도는 공간 패널의 수치와 따뜻함·시원함을 암시하는 절제된 시각 효과로 표시한다.
7. 중지·만료 시 기본 공간 프리셋으로 즉시 돌아간다.

초기에는 실시간으로 모든 3D 속성을 계산하기보다 다음 네 개 장면을 준비하는 것이 안전하다.

- 기본 호텔 방
- 회복 환경
- 집중 환경
- 안정 환경

### Go/No-Go 체크포인트

핵심 웹/모바일 흐름이 완성된 뒤 반나절에서 하루 안에 아래 프로토타입이 되지 않으면 XR은 발표 영상 또는 목업으로 내린다.

- XR 에뮬레이터 실행
- 기본 공간과 회복 공간 전환
- 공통 JSON 프로필 수신
- 적용·복원 버튼 동작
- 발표 화면 미러링

---

## 6. P1 — 핵심 완성 후 추가

- 실제 스마트 전구 한 종류 연동
- 항목별 동의 철회와 저장 데이터 삭제
- 조명·온도·사운드별 자동 적용 범위 설정
- 운영자용 세션 상태 화면
- 적용 성공·부분 실패·복원 여부 집계
- 네트워크·기기 실패 시뮬레이션
- 카메라를 이용한 실제 QR 스캔
- Android XR 공간 프리셋 적용

---

## 7. MVP에서 제외할 기능

- 다중 사용자 평균 최적화
- 멤버십 최고 등급 우선 적용
- NFC와 위치 기반 자동 체크인
- 실제 웨어러블 계정 연결
- 호텔 PMS·예약·결제 연동
- 여러 제조사의 IoT 연동
- 복잡한 운영자 권한 및 다지점 관리
- 실제 AI 모델 학습
- 운영 분석 대시보드 전체
- 음식 주문, 샤워 제어 등 추가 자동화

다중 사용자와 등급 우선 정책은 기술 구현 전에 프라이버시·공정성·운영 정책 검증이 필요하다.

---

## 8. 권장 개발 순서

```text
공통 데이터 모델
→ 더미 데이터
→ 규칙 단독 기준선
→ Agents SDK 신호 맥락 판단
→ 결정론적 환경 프로필 생성
→ 기본 방 시뮬레이터 적용·복원
→ 사용자 피드백 보정
→ QR 공간 사양 로드
→ 호텔 프로필 변환
→ 일회성 세션·만료
→ 실제 전구 연동
→ Android XR 쇼케이스
→ 운영자 최소 화면
```

각 단계는 이전 단계가 시연 가능한 상태가 된 뒤 착수한다. XR 개발 때문에 핵심 사용자 여정이 미완성 상태로 남지 않도록 한다.

---

## 9. 최종 완료 기준

다음 흐름이 끊기지 않고 실행되면 MVP 완료로 판단한다.

1. 사용자가 더미 컨디션을 선택한다.
2. 구분 가능한 환경 프로필과 추천 이유가 생성된다.
3. 집 시뮬레이터 또는 스마트 전구에 적용된다.
4. 사용자가 값을 수정하고 같은 조건의 다음 추천이 달라진다.
5. QR로 호텔 공간에 체크인한다.
6. 집 프로필이 호텔 지원 범위로 변환된다.
7. 전달 정보에 생체정보가 없음을 확인한다.
8. 호텔 시뮬레이터 또는 XR 공간에 30초 안에 적용된다.
9. 즉시 중지와 기본값 복원이 성공한다.
10. 체크아웃 후 세션을 다시 사용할 수 없다.

비개발 완료 기준:

- 사용자 인터뷰 5건 이상
- 제휴 공간 운영자 인터뷰 또는 PoC 의향 1건 이상
- 3분 내외 발표 시나리오
- XR 실패 시 사용할 일반 화면 기반 백업 데모 또는 녹화 영상

---

## 10. Android XR 참고자료

- [Android XR 가상 헤드셋·글래스 AVD 생성](https://developer.android.com/develop/xr/jetpack-xr-sdk/run/create-avds/xr-headsets-glasses)
- [기존 Android 앱에 XR 기능 추가](https://developer.android.com/develop/xr/jetpack-xr-sdk/add-xr-to-existing)
- [Jetpack XR 공간 환경 추가](https://developer.android.com/develop/xr/jetpack-xr-sdk/add-environments)
- [Android XR 공간 UI 디자인](https://developer.android.com/design/ui/xr/guides/spatial-ui)
- [Android XR 공간 환경 디자인](https://developer.android.com/design/ui/xr/guides/environments)
