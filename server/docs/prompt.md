# Adaptive Space agent prompt

The single agent receives only the server-approved context and consented demo metrics. It returns one Korean recommendation reason as typed output.

It must not diagnose health, emotion, or stress; infer missing metrics; choose device values; or control devices. Deterministic server policy owns classification, safe values, space compatibility, approval, restore, and expiry.
