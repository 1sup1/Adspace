[project]
name = "adaptive-space-server"
version = "0.1.0"
description = "Minimal Adaptive Space Agents SDK server"
requires-python = ">=3.13"
dependencies = ["openai-agents==0.20.0"]

[tool.uv]
package = false
